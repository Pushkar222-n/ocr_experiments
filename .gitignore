# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv/

# model weights & hf/paddle downloads
hf_cache/
*.safetensors
*.gguf
*.pdparams

# regenerable intermediates: rasterized pages, vllm server logs
work/

# OCR results are derived from customer PDFs — this repo is public, so nothing under
# outputs/ is committed. Carry them between pods with the resume bundle instead:
#   zip -rq ocr_resume_bundle.zip outputs data
# Unzipping it at the repo root restores every checkpoint, so runs resume in place.
outputs/

# resume bundles
*.zip

.ipynb_checkpoints/
data

