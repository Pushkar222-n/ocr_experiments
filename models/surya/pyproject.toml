[project]
name = "surya-run"
version = "0.0.0"
requires-python = ">=3.11,<3.13"
dependencies = [
    "ocr-harness",
    "surya-ocr>=0.21",
    "vllm",
    "markdownify",
]

[tool.uv.sources]
ocr-harness = { path = "../../harness", editable = true }
