[project]
name = "dots-ocr-run"
version = "0.0.0"
requires-python = ">=3.11,<3.13"
dependencies = [
    "ocr-harness",
    "vllm>=0.11.0",
    "pillow>=10",
]

[tool.uv.sources]
ocr-harness = { path = "../../harness", editable = true }
