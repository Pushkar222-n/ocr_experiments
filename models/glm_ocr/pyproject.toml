[project]
name = "glm-ocr-run"
version = "0.0.0"
requires-python = ">=3.12,<3.13"
dependencies = [
    "ocr-harness",
    "glmocr[selfhosted]",
]

[tool.uv.sources]
ocr-harness = { path = "../../harness", editable = true }
